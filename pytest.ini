[pytest]
testpaths = backend/tests aqp_eval
addopts = -q
markers =
    integration: end-to-end test needing a real database (run with -m integration)
    benchmark: performance benchmark, not a correctness test (run with -m benchmark)
